---
title: "Custom Fields"
description: "Custom Fields let you extend Bindbee's unified models with additional fields specific to your use case. They make it possible to surface any value from a third-party API response - alongside the standard unified schema - by mapping it through a JMESPath expression."
---

## Overview

A unified model has a fixed schema (e.g. an `employee` always exposes `first_name`, `email`, etc.). Custom Fields let you extend that schema with extra attributes - for example `guardian_mobile` - and tell Bindbee how to populate them by pointing a [JMESPath](https://jmespath.org/) expression at the raw upstream payload.

<Info>
  For how custom fields actually work - fields vs mappings, integration vs
  connector scope and which wins, freshness, and failure modes - see [Custom Fields: The
  Mental Model](/explanation/custom-fields-mental-model). This page covers
  where to configure them and how to work with them day to day.
</Info>

## Choosing how to manage Custom Fields

You can manage Custom Fields in two ways. Both create the same underlying configuration and can coexist - pick whichever fits the task.

<CardGroup cols={2}>
  <Card title="Dashboard Configuration" icon="app-window" href="/how-to/configure-custom-fields-dashboard">
    Visual flow inside the Bindbee Dashboard. Best for ad-hoc setup,
    exploring upstream payloads interactively, and one-off field changes.
  </Card>
  <Card title="API Configuration" icon="code" href="/how-to/configure-custom-fields-api">
    Programmatic flow for automation, infrastructure-as-code, replicating
    configuration across environments, or syncing with internal admin tools.
  </Card>
</CardGroup>

## Using Custom Fields in Responses

Once a field has at least one mapping that resolves on a connector, request it back in the unified API response by adding `include_custom_fields=true`:

```http
GET https://api.bindbee.dev/api/hris/v1/employees?include_custom_fields=true
```

Fields arrive under a `custom_fields` object, tagged `source: "API"` or `source: "DASHBOARD"` depending on how they were created - both behave identically and appear together in lookups and responses. See [Fields are opt-in on read](/explanation/custom-fields-mental-model#fields-are-opt-in-on-read) for the response shape, and [Failure modes](/explanation/custom-fields-mental-model#failure-modes) for why a field can come back as `"INVALID_JSON_PATH"` and how to catch it before it ships - via the dashboard's preview button or the API's [`/custom-fields/preview`](/how-to/configure-custom-fields-api#step-3-validate-the-jmespath-optional-but-recommended) endpoint.

## Best Practices

1. **Naming Conventions**

   - Use clear, descriptive names.
   - Follow snake_case formatting.
   - Avoid generic names like `custom1`, `custom2`.

2. **JMESPath Expressions**

   - Always preview an expression before saving it.
   - Consider data type consistency across connectors of the same integration.

3. **Integration vs Connector Level Mapping**

   - Use **Integration Level** mappings for data that's consistent across all connectors of an integration.
   - Use **Connector Level** mappings for connector-specific overrides.
   - Review existing **Integration Level** mappings before creating connector-level duplicates.

4. **Automation**

   - Use the [API Configuration](/how-to/configure-custom-fields-api) flow to keep custom field configuration in version control or replicate it across environments (dev → staging → prod).
   - The `configuration` endpoint is a good health check - run it after provisioning a new connector to confirm every expected field has a mapping.

For more details on writing JMESPath expressions, see the [JMESPath Documentation](https://jmespath.org/).
