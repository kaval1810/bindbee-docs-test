---
title: "Model Availability Matrix"
sidebarTitle: "Model Availability Matrix"
description: "Check Model level support across all integrations supported by Bindbee"
icon: "table"
mode: "wide"
---

import { ModelSupportMatrix } from "/components/model-support-matrix.jsx";

This matrix shows which **Bindbee models each integration returns**, at the model level. Filter by provider, connection type or model, then copy the link to share the exact view you're looking at.

Support varies by connector - and on SFTP connectors, by the individual customer's export template. To confirm what a *specific* connector returns, see [Check a connector's actual coverage](/get-started/integrations#check-a-connectors-actual-coverage).

<ModelSupportMatrix />
