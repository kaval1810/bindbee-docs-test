---
title: "Quick start"
description: "What you will build in this tutorial, what you need in front of you before you start, and what the tutorial deliberately leaves out."
---

This is a single tutorial in seven lessons - work through them in order, since each one depends on the state the previous one left behind.

## What you will build

By the end, employee records from a linked HRIS will be arriving in your own code. That includes:

- A **connector** you created and authorized against a test HRIS integration
- A completed **sync**, with a way to get notified when it finishes
- **Employee data** read from the Bindbee API in the unified schema
- **One field the unified model does not carry**, mapped out of the raw payload using a custom field
- A **webhook** that tells your application when that data changes

That is the whole thing.

## What you will need

<Info>
  Every item below is something to have open or on hand before lesson 1. If you
  are missing any of them, resolve it first.
</Info>

| You need | How to get it |
| --- | --- |
| Access to the Bindbee Dashboard | Provisioned by the Bindbee team during onboarding. If you cannot sign in, reach out to us or email us on [support@bindbee.dev](mailto:support@bindbee.dev) |
| A **development** environment API key | **Settings → API Keys**, from the Development block. See [Development environment](/how-to/setup/environments/development). |
| Credentials for a test HRIS integration | Keep the HRIS sandbox or testing environment for validating the connection. |

<Warning>
  Of the three, the test HRIS account is the one most likely to cost you real
  time - signing up for a sandbox with a supported system can take longer than
  lessons 1 and 2 combined. Start that signup now if you don't already have
  one, rather than discovering the wait at lesson 3.
</Warning>

## What this tutorial does not cover

Leaving these out keeps each lesson a single path with no branching decisions. They are real tasks - just ones you will handle later.

<AccordionGroup>
  <Accordion title="Embedding Magic Link inside your own product">
    In lesson 3 you create a connector from the Dashboard, which is the fastest
    route to a working sync. Embedding the linking flow in your own UI is a
    separate job you will do before you onboard real customers.
  </Accordion>

  <Accordion title="Preparing a real customer's source system">
    Test tenants come with the permissions already granted. Real customers do
    not - and missing permissions are the single most common reason fields
    arrive empty. That is covered in the [Go-live
    checklist](/how-to/setup/go-live-checklist).
  </Accordion>

  <Accordion title="Going to production">
    Everything here happens in the development environment. The steps for a
    production connection are the same - the only difference is using a
    production API key instead of a development one. See [Production
    environment](/how-to/setup/environments/production) for what carries across
    environments and what does not.
  </Accordion>

  <Accordion title="Writing data back to the source system">
    This tutorial only reads. Bindbee supports writes on several models - see
    the [API reference](/api-reference) for the endpoints that accept `POST`.
  </Accordion>
</AccordionGroup>

## Worth reading first

The lessons teach each idea as you need it, so you can start right now. If you would rather see the whole model first, [Core concepts](/explanation/core-concepts) is a ten-minute read covering environments, API keys, connectors, connector tokens, syncs, unified models, and custom fields - it answers most of what lessons 1 through 3 will raise.

## The lessons

<Steps>
  <Step title="Set up your environment and keys">
    Development versus production, and which of the two tokens goes on which
    request. [Start here →](/get-started/tutorials/set-up-your-environment)
  </Step>
  <Step title="Scope the data you need">
    Choose the models and fields your product reads. This decision shapes
    everything downstream. [Lesson 2 →](/get-started/tutorials/scope-your-data)
  </Step>
  <Step title="Connect a test account">
    Create a connector against a sandbox HRIS tenant and authorize it.
    [Lesson 3 →](/get-started/tutorials/connect-a-test-account)
  </Step>
  <Step title="Run your first sync">
    Development connectors do not sync on a schedule. You will trigger this one
    yourself and watch it complete. [Lesson 4 →](/get-started/tutorials/run-your-first-sync)
  </Step>
  <Step title="Read employee data from the API">
    Your first authenticated call that returns real records.
    [Lesson 5 →](/get-started/tutorials/read-employee-data)
  </Step>
  <Step title="Add a field the unified model does not have">
    Find the value in the raw payload, map it, and see it appear on the
    unified object. [Lesson 6 →](/get-started/tutorials/add-a-custom-field)
  </Step>
  <Step title="Know when data changes">
    Subscribe to a webhook so your application stops polling.
    [Lesson 7 →](/get-started/tutorials/receive-a-webhook)
  </Step>
</Steps>

## If you get stuck

Each lesson ends with the failures that lesson can cause. These six account for most of them - worth skimming now so you recognize them when they happen.

| Symptom | Cause | Where it's fixed |
| --- | --- | --- |
| `401 Unauthorized` | Missing the `Bearer ` prefix, or a key from the other environment | [Lesson 1](/get-started/tutorials/set-up-your-environment) |
| `404 Not Found` on a connector endpoint | Connector paths are category-scoped - `/api/hris/v1/connectors`, not `/api/v1/connectors` | [Lesson 1](/get-started/tutorials/set-up-your-environment) |
| `403` on `/employees` while `/connectors` works | The `X-Connector-Token` header is missing | [Lesson 3](/get-started/tutorials/connect-a-test-account) |
| `items` is `[]` and nothing is obviously wrong | Development connectors never sync on their own | [Lesson 4](/get-started/tutorials/run-your-first-sync) |
| A field you expected is `null` | Out of scope, not permitted by the source system, or not carried by the unified model | [How Bindbee decides what data you get](/explanation/what-data-you-get) |
| `429 Too Many Requests` | Per-connector rate limit exceeded | [Rate limits](/api-reference/basics/rate-limits) |

<Check>
  Everything in this tutorial happens in the development environment against a
  test tenant. No real customer data is touched at any point.
</Check>

Ready? [Set up your environment and keys →](/get-started/tutorials/set-up-your-environment)
